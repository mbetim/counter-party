import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#51E5FF',
    },
    background: {
      default: '#2274A5',
    },
    text: {
      primary: '#FFFFFF',
      secondary: '#EAEAEA',
    },
  },
});
